import { HideOnHoverDirective } from './hide-on-hover.directive';

describe('HideOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new HideOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});

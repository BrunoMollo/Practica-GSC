import { ExcludeWeekendsDirective } from './exclude-weekends.directive';

describe('ExcludeWeekendsDirective', () => {
  it('should create an instance', () => {
    const directive = new ExcludeWeekendsDirective();
    expect(directive).toBeTruthy();
  });
});
